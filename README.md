# messin around with spotify 

1. clone this repo
2. add `client_id.txt` and `client_secret.txt` with your personal `client_id` and `client_secret`
3. run `python3 search.py <artist_name>`

super early, still formatting / exploring
